import { TimeToReadPipe } from './time-to-read.pipe';

describe('TimeToReadPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToReadPipe();
    expect(pipe).toBeTruthy();
  });
});
