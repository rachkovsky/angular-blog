import { NgIfAdminDirective } from './ng-if-admin.directive';

describe('NgIfAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfAdminDirective();
    expect(directive).toBeTruthy();
  });
});
